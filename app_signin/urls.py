from django.urls import path
from .views import *

urlpatterns = [
    # path('', SigninView.as_view()),
    # path('RefreshToken/', SigninTokenRefreshView.as_view()),
    # path('Signout/', SignoutView.as_view()),
]
